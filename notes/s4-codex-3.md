The S4 widget is a pocket-sized field calibrator for tiny machines that drift out of tune during long test runs.
It has a blue ceramic shell, partly because the color makes it easy to find on a crowded bench.
The dial is deliberately stiff so a bumped tool bag does not change the setting by accident.
Nobody uses it for production work; it is mostly a small, stubborn fixture for experiments that need repeatable weirdness.
